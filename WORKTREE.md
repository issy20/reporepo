# Git Worktree を用いた開発ガイド

本プロジェクトでは、同じリポジトリ内の複数ブランチを異なるディレクトリに展開して並行開発ができる **`git worktree`** を採用しています。以下に開発の流れとコマンド、およびディレクトリ設計についてまとめます。

---

## 1. ディレクトリ構成

親ディレクトリ `reporepo/` の配下に、リポジトリ本体である `main/` と、各開発ブランチごとのワークツリーを並列に配置します。

```text
reporepo/ (親フォルダ - Git管理外)
├── main/                 (本尊: .git フォルダと main ブランチが存在)
├── feature-auth/         (ワークツリー: 認証機能開発用)
└── bugfix-tui-input/     (ワークツリー: TUIバグ修正用)
```

---

## 2. 開発の基本フロー

新しい機能の開発やバグ修正を行う際の標準的な流れです。

### 2.1 ワークツリーの作成
1. 本体の `main/` ディレクトリに移動します。
   ```bash
   cd main
   ```
2. 最新の `main` ブランチに更新します。
   ```bash
   git checkout main
   git pull origin main
   ```
3. 新しいワークツリーを作成します（例として `feature-auth` ブランチとディレクトリを作成）。
   ```bash
   git worktree add ../feature-auth -b feature-auth
   ```
   * ※ `../` を付けることで、`main/` の外（親ディレクトリの直下）にワークツリーが作られます。

### 2.2 ワークツリーでの開発と送信
1. 作成されたワークツリーへ移動します。
   ```bash
   cd ../feature-auth
   ```
2. 通常通りコードを実装し、ビルドやテストを行います。
3. そのディレクトリのままコミットとプッシュを行います。
   ```bash
   git add .
   git commit -m "feat: 認証機能のベース実装"
   git push origin feature-auth
   ```

### 2.3 開発完了後のワークツリーのクリーンアップ
プルリクエストがマージされるなどして、そのブランチでの作業が完全に終了したら、ワークツリーを削除してディレクトリを整理します。

1. 本体の `main/` ディレクトリ（または他の生存しているディレクトリ）に移動します。
   * ※ 削除対象のディレクトリ内にいる状態では削除できません。
   ```bash
   cd ../main
   ```
2. `git worktree remove` コマンドでワークツリーを安全に削除します。
   ```bash
   git worktree remove ../feature-auth
   ```
   * これにより、`../feature-auth` ディレクトリと、Git の内部管理データからワークツリー情報が自動で削除されます。
3. 必要に応じて、リモートでマージ済みのローカルブランチも削除します。
   ```bash
   git branch -d feature-auth
   ```

---

## 3. ディレクトリ命名規則のガイドライン

並行して複数のワークツリーを運用する場合、以下のルールに従って命名します。

| 種類 | 命名規則のパターン | ディレクトリ（ブランチ）名例 |
| :--- | :--- | :--- |
| **新機能追加** | `feature-<機能名>` | `feature-core-config`, `feature-client-ai` |
| **バグ修正** | `bugfix-<修正内容やIssue番号>` | `bugfix-tui-input-collision`, `bugfix-issue-42` |
| **リファクタ** | `refactor-<対象>` | `refactor-store-json` |
| **環境固定用** | `<環境名>` | `staging`, `develop` |

---

## 4. 開発時の注意点

*   **Git コマンドの実行場所**
    *   親ディレクトリである `reporepo/` の直下は Git の管理外です。`git` で始まるコマンドは必ず `main/` または作成した各ワークツリー（`feature-auth/` など）の内部で実行してください。
*   **`.gitignore` の管理**
    *   `.gitignore` は Git の管理対象ファイルです。[main/.gitignore](.gitignore) を修正してコミットすれば、新規作成するすべてのワークツリーに自動で適用されます。
    *   親ディレクトリ直下に個別の `.gitignore` を配置する必要はありません。
*   **強制作業ディレクトリの削除**
    *   手動でワークツリーフォルダを `rm -rf` などで消すと、Git の管理データと不整合が起きる原因になります。必ず `git worktree remove` を使用して削除してください。
    *   万が一手動でフォルダを削除してしまった場合は、`git worktree prune` を実行して管理データをクリーンアップしてください。
